function Load (source ,callback){
    var imgs = {};
    var imgLength = source.length;
    for(var i =0;i<imgLength;i++){
        switch (source[i].className) {
            case "sky":
                Sky
                break;
            case "tunnel":
                break;
            case "ground":
                break;
            case "birds":
                break;
            default:
                break;

        }
    }
}